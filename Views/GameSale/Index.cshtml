@model IEnumerable<GameLibrary.Models.GameSale>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CustomerId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GameId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateOfSell)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CustomerId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GameId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateOfSell)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GameSaleId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GameSaleId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GameSaleId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
